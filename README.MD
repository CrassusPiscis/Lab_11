Супер-крутой проект Артура и Саши из команды Бар-Ахлит. Версия три. мы нашлись.

# 📚 Книжный Каталог

Веб-приложение для управления личной библиотекой, разработанное в рамках командного проекта. Позволяет пользователям регистрироваться, вести учет своих книг и просматривать профили других читателей.

## 🚀 Как запустить проект

Для запуска проекта на локальном компьютере выполните следующие шаги:

1. **Клонируйте репозиторий:**
```bash
git clone https://github.com/CrassusPiscis/Lab_11
cd Lab11

```

2. **Создайте и активируйте виртуальное окружение:**
```bash
python -m venv .venv
source .venv/bin/activate  # Для Linux/macOS
.venv\Scripts\activate     # Для Windows

```


3. **Установите зависимости:**
```bash
pip install flask flask-sqlalchemy flask-migrate flask-wtf flask-login email-validator

```


4. **Инициализируйте базу данных:**
```bash
flask db upgrade

```


5. **Запустите приложение:**
```bash
python run.py

```


Приложение будет доступно по адресу: `http://127.0.0.1:5000`

---

## 🛠 Архитектура приложения (MVC)

Проект реализован с использованием паттерна **MVC (Model-View-Controller)**:

### 1. Модель (Model) — `app/models.py`

Используется **SQLAlchemy** для работы с базой данных SQLite.

* **User**: Хранит данные о пользователях (ID, имя, хеш пароля). Реализует методы защиты паролей через `werkzeug.security`.
* **Book**: Содержит информацию о книгах (название, автор, год, жанр, статус чтения) и внешний ключ `user_id` для связи с владельцем.

### 2. Вид (View) — `app/templates/`

Шаблонизатор **Jinja2** в сочетании с **Bootstrap 5**.

* Реализована адаптивная верстка и динамическое отображение элементов (например, кнопки управления книгами видны только их владельцу).
* Используются Flash-сообщения для уведомления пользователя об успешных действиях.

### 3. Контроллер (Controller) — `app/routes.py`

Логика приложения и маршрутизация.

* Обработка форм через **Flask-WTF** (валидация данных, защита от CSRF).
* Управление сессиями пользователей с помощью **Flask-Login**.
* Реализация CRUD-операций для книг и публичных профилей.

---

## 👥 Командная работа

Работа над проектом была распределена между двумя участниками. Для обеспечения параллельной разработки использовалась методология GitLab Flow.

Взаимодействие происходило через Issues Гитхаба и прямую связь.

## 💿 Ссылка на обзорное видео на Яндекс Диске

https://disk.yandex.ru/i/Ahg6XOK9QekuGA
